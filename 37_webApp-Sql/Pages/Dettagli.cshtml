@page 
@model DettagliModel

@{
    ViewData["Title"] = "Dettagli";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Prezzo</th>
            <th>Categoria</th>
        </tr>
    </thead>
    <tbody>
         <tr>
                <td>@Model.Prodotto.Id</td>
                <td>@Model.Prodotto.Nome</td>
                <td>@Model.Prodotto.Prezzo</td>
                <td>@Model.Prodotto.CategoriaNome</td>
                <td>
                    
                     <a asp-page="Edit" class="btn btn-warning" asp-route-id="@Model.Prodotto.Id">Edit</a>
                     <a asp-page="Delete" class="btn btn-danger" asp-route-id="@Model.Prodotto.Id">Delete</a>
                </td>
            </tr>
    </tbody>
</table>
